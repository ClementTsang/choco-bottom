name: Check

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - master

jobs:
  # Checks install
  install:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - windows-2019

    steps:
      - uses: actions/checkout@v2

      - name: Check
        run: |
          choco pack
          choco install bottom -dv -s .
          btm -V;